// import { selectCulturalPlacesListPageDomain } from '../selectors';

describe('selectCulturalPlacesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
